import { TokenProvider } from './linkedin-client.js';
import { ServerConfig } from './types.js';
export declare class LinkedInMCPServer {
    private server;
    private linkedInClient;
    private logger;
    constructor(config: ServerConfig, tokenProvider?: TokenProvider);
    private setupTools;
    start(): Promise<void>;
    stop(): Promise<void>;
}
//# sourceMappingURL=server.d.ts.map